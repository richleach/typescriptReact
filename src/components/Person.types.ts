//you can load all of your prop type definitions into one file then import into component when needed. 
//Don't forget to export them!

export type PersonProps = {
    name: {
        first: string,
        last: string
    }
}
